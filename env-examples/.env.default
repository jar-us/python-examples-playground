# Default Environment Configuration
# These are the base settings that can be overridden by specific environments

# Database Configuration
DATABASE_URL=sqlite:///default.db

# API Configuration
API_KEY=default_key_please_change

# Application Settings
DEBUG=False
PORT=8080
APP_ENV=development

# Email Settings (defaults for development)
SMTP_HOST=localhost
SMTP_PORT=25
SMTP_USER=test
SMTP_PASSWORD=test

# Logging
LOG_LEVEL=INFO
